x-common-settings: &common-settings
  build: .
  restart: always
  volumes:
    - .:/app
    - ./tg.py:/app/tg.py

services:
  club_bot:
    <<: *common-settings
    container_name: club_bot
    env_file:
      - ./envs/env.env
